<UserControl x:Class="FrostedTXT.App.Controls.DropdownMenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="TopRightMenuButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource FrostFocusVisualStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <Border x:Name="MenuButtonHover"
                                    CornerRadius="17"
                                    Background="{StaticResource FrostSurfaceHoverBrush}"
                                    Opacity="0" />
                            <TextBlock Text="..."
                                       FontSize="16"
                                       Margin="0,-2,0,0"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource FrostTextPrimaryBrush}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="MenuButtonHover"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1"
                                                             Duration="0:0:0.14" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="MenuButtonHover"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0"
                                                             Duration="0:0:0.14" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="MenuButtonHover" Property="Opacity" Value="1" />
                                <Setter TargetName="MenuButtonHover" Property="Background" Value="{StaticResource FrostSurfaceActiveBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button x:Name="MenuButton"
                Style="{StaticResource TopRightMenuButtonStyle}"
                Click="MenuButton_OnClick">
            <Button.ContextMenu>
                <ContextMenu x:Name="MenuPopup"
                             PlacementTarget="{Binding ElementName=MenuButton}">
                    <MenuItem Header="New Tab" Click="NewTab_OnClick" />
                    <MenuItem Header="Open..." Click="Open_OnClick" />
                    <Separator />
                    <MenuItem Header="Save" Click="Save_OnClick" />
                    <MenuItem Header="Save As..." Click="SaveAs_OnClick" />
                    <Separator />
                    <MenuItem Header="Settings..." Click="Settings_OnClick" />
                    <MenuItem Header="About..." Click="About_OnClick" />
                    <Separator />
                    <MenuItem Header="Exit" Click="Exit_OnClick" />
                </ContextMenu>
            </Button.ContextMenu>
        </Button>
    </Grid>
</UserControl>
