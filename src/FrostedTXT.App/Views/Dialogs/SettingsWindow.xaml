<Window x:Class="FrostedTXT.App.Views.Dialogs.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="420"
        Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Font" VerticalAlignment="Center" />
        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding AvailableFonts}"
                  SelectedItem="{Binding FontFamilyName, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Font Size" VerticalAlignment="Center" />
        <Slider Grid.Row="1" Grid.Column="1" Minimum="10" Maximum="28"
                Value="{Binding FontSizeBase, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Zoom" VerticalAlignment="Center" />
        <Slider Grid.Row="2" Grid.Column="1" Minimum="0.8" Maximum="2.0"
                Value="{Binding ZoomLevel, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Translucency" VerticalAlignment="Center" />
        <Slider Grid.Row="3" Grid.Column="1" Minimum="0.15" Maximum="0.75"
                Value="{Binding BackgroundOpacity, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Blur Mode" VerticalAlignment="Center" />
        <ComboBox Grid.Row="4" Grid.Column="1"
                  ItemsSource="{Binding BlurModes}"
                  SelectedItem="{Binding BlurMode, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Word Wrap" VerticalAlignment="Center" />
        <CheckBox Grid.Row="5" Grid.Column="1"
                  IsChecked="{Binding WordWrapEnabled, UpdateSourceTrigger=PropertyChanged}"
                  VerticalAlignment="Center" />

        <Border Grid.Row="6" Grid.ColumnSpan="2"
                Margin="0,12,0,12"
                CornerRadius="8"
                Padding="12"
                Background="{Binding TintBrush}">
            <TextBlock Text="Live preview: FrostedTXT"
                       FontFamily="{Binding FontFamilyName}"
                       FontSize="{Binding FontSizeBase}" />
        </Border>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="88" Click="Save_OnClick" />
            <Button Content="Close" Width="88" Click="Close_OnClick" />
        </StackPanel>
    </Grid>
</Window>
